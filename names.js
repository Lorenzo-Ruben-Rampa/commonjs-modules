// Creo la funzione
function firstFunction(firstName, lastName) {
    return {
        firstName: firstName,
        lastName: lastName
    };
}

// Esporto la funzione
module.exports = firstFunction;